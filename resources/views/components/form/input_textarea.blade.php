<input
    class="block w-full border-0 pt-2.5 text-xs font-medium placeholder-gray-900 focus:ring-0"
    type="text"
    name="{{ $name }}"
    id="{{ $name }}"
    aria-label="{{ $name }}"
    placeholder="{{ $name }}"
/>
